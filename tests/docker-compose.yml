version: "3.8"

services:
  tftp-tests:
    build:
      context: ../../..
      dockerfile: crates/usg-tftp/tests/Dockerfile.integration
    container_name: usg-tftp-tests
    # Show test output
    tty: true
    stdin_open: true
  tftp-benchmarks:
    build:
      context: ../../..
      dockerfile: crates/usg-tftp/tests/Dockerfile.bench
    container_name: usg-tftp-benchmarks
    # Show test output
    tty: true
    stdin_open: true
